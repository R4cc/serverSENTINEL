{
  "name": "serversentinel",
  "version": "1.10.1",
  "private": true,
  "packageManager": "npm@11.17.0",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm run check:versions && npm --workspace server run build && npm --workspace web run build",
    "test": "npm --workspace server run test && npm --workspace web run test",
    "test:console": "npm run build && node scripts/console-smoke.mjs",
    "test:mobile": "npm run build && node scripts/mobile-ui-smoke.mjs",
    "test:overview": "npm run build && node scripts/overview-timeline-smoke.mjs",
    "test:smoke": "npm run build && node scripts/console-smoke.mjs && node scripts/mobile-ui-smoke.mjs && node scripts/overview-timeline-smoke.mjs",
    "test:server": "npm --workspace server run test",
    "test:web": "npm --workspace web run test",
    "typecheck": "npm run check:versions && npm --workspace server run typecheck && npm --workspace web run typecheck",
    "check:versions": "node scripts/check-version-consistency.mjs",
    "demo:reset": "npm --workspace server run demo:reset",
    "screenshots:update": "node scripts/update-readme-screenshots.mjs",
    "start": "npm --workspace server run start"
  },
  "devDependencies": {
    "@fontsource-variable/cascadia-code": "^5.3.0",
    "playwright": "1.62.1"
  },
  "overrides": {
    "vite": "^8.2.1",
    "esbuild": "^0.28.1"
  }
}
